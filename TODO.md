# TODO

- [ ] Update `frontend/src/pages/Admin.js` markup/classes to better match the personal profile page layout structure (left sidebar panel + right content panel).
- [ ] Update `frontend/src/pages/CartAndAndmin.css` to add/adjust CSS selectors for Admin so it reuses the same visual language as `Profile.css` (yellow/orange left panel, beige right panel, tab-like nav buttons, consistent typography/buttons).
- [ ] Sanity check: ensure Admin renders without missing class styling and active nav state looks correct.

